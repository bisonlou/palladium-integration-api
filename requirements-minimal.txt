# Minimal requirements for quick testing
# Use this if you're having trouble with the full requirements.txt

Flask==1.1.1
Flask-Cors==3.0.8
Flask-SQLAlchemy==2.4.1
psycopg2-binary==2.8.4
python-dotenv==0.10.5
PyJWT==1.7.1
Werkzeug==0.16.1
alembic==1.3.3

# For password hashing
Werkzeug==0.16.1

# For email functionality (built into Python, but explicit for clarity)
# smtplib is built-in
# email.mime is built-in